---
const links = [
  { name: 'Inicio', href: '/comsoc-web/' },
  { name: 'Nosotros', href: '/comsoc-web/nosotros' },
  { name: 'Eventos', href: '/comsoc-web/eventos' },
  { name: 'Proyectos', href: '/comsoc-web/proyectos' },
  { name: 'Convocatorias', href: '/comsoc-web/convocatorias' },
  { name: 'Tienda', href: '/comsoc-web/tienda' },
];
---

<div
  id="mobile-menu-overlay"
  class="fixed inset-0 z-50 bg-black/80 backdrop-blur-xl opacity-0 pointer-events-none transition-all duration-300 md:hidden flex flex-col justify-between p-6"
>
  <div class="flex items-center justify-between border-b border-white/10 pb-4">
    <a href="/" class="flex items-center gap-3">
      <img src="/images/logo-comsoc.svg" alt="IEEE ComSoc" class="h-9 w-auto" />
    </a>
    <button
      id="close-mobile-menu"
      type="button"
      class="p-2 rounded-xl bg-white/5 border border-white/10 text-gray-300 hover:text-white"
      aria-label="Cerrar menú"
    >
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  </div>

  <nav class="flex flex-col gap-4 py-8">
    {links.map((link) => (
      <a
        href={link.href}
        class="text-xl font-medium tracking-wide text-gray-300 hover:text-[#22d3ee] transition-colors py-2 px-3 rounded-lg hover:bg-white/5"
      >
        {link.name}
      </a>
    ))}
  </nav>

  <div class="pt-6 border-t border-white/10 flex flex-col gap-3">
    <a
      href="/comsoc-web/convocatorias"
      class="w-full py-3 text-center font-semibold rounded-xl bg-gradient-to-r from-[#155dfc] to-[#22d3ee] text-white shadow-lg shadow-blue-500/25"
    >
      ¡Únete al Capítulo!
    </a>
  </div>
</div>

<script>
  const openBtn = document.getElementById('open-mobile-menu');
  const closeBtn = document.getElementById('close-mobile-menu');
  const overlay = document.getElementById('mobile-menu-overlay');

  if (openBtn && closeBtn && overlay) {
    const toggleMenu = (open: boolean) => {
      if (open) {
        overlay.classList.remove('opacity-0', 'pointer-events-none');
        overlay.classList.add('opacity-100', 'pointer-events-auto');
        document.body.style.overflow = 'hidden';
      } else {
        overlay.classList.add('opacity-0', 'pointer-events-none');
        overlay.classList.remove('opacity-100', 'pointer-events-auto');
        document.body.style.overflow = '';
      }
    };

    openBtn.addEventListener('click', () => toggleMenu(true));
    closeBtn.addEventListener('click', () => toggleMenu(false));

    overlay.querySelectorAll('a').forEach((link) => {
      link.addEventListener('click', () => toggleMenu(false));
    });
  }
</script>
